# Elixir and Erlang version configuration for Gigalixir
# Gigalixir uses this file to determine which versions to use when building your app

erlang_version=27.0.1
elixir_version=1.19.0

# Gigalixir automatically runs:
# - mix deps.get
# - mix compile
# - mix assets.deploy (detected from mix.exs)
# - Database migrations (if DATABASE_URL is set)

